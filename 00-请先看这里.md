# 黑马记账：请先看这里

## 你现在只需要记住一个入口

双击项目根目录中的：

```text
00-点我打开黑马记账-当前最新版.cmd
```

它是当前唯一推荐启动入口。它会优先打开已经生成的1.4.0免安装预览版；如果预览文件以后被清理，它会尝试从项目源码启动，并在无法启动时给出明确提示。

## 当前文件分别是什么

| 名称 | 当前版本 | 是什么 | 现在是否使用 |
| --- | --- | --- | --- |
| `00-点我打开黑马记账-当前最新版.cmd` | 1.4.0 | 唯一推荐启动按钮 | **是，双击它** |
| `可直接打开-黑马记账-1.4.0预览版` | 1.4.0 | 当前免安装预览程序的完整文件夹 | 是，但不要拆散或只移动里面的EXE |
| `可直接打开-黑马记账-1.3.0预览版` | 1.3.0 | 上一版免安装预览 | **否，属于旧版本** |
| `release` | 1.0.0和1.1.0 | 以前生成的安装包、便携版和解压版 | **否，属于旧版本** |
| `src` | 1.4.0源码 | 开发人员使用的程序原材料 | 不要直接修改或移动 |
| `node_modules` | 开发依赖 | 启动和构建需要的程序零件 | 不要手工修改 |
| `.git` | 全部存档历史 | Git的“游戏存档库” | 不要删除或修改 |

## “预览版、免安装版、安装版、EXE”分别是什么意思

- **预览版**：表示当前功能已经能使用和测试，但还在等你亲自体验确认。这是版本状态，不是某一种文件格式。
- **免安装版**：不需要安装到Windows，保留完整文件夹就能打开。不能只复制里面一个EXE，因为它还需要旁边的文件。
- **安装版**：双击安装程序后，会通过安装向导写入电脑，并可能创建桌面和开始菜单快捷方式。
- **EXE**：Windows可执行文件的后缀。安装程序、单文件便携版和免安装目录里的主程序都可能叫EXE，所以不能只看到EXE就判断是不是最新版。
- **源码启动**：使用 `npm.cmd run dev` 让开发工具临时运行最新代码，适合开发检查，但没有直接双击免安装版方便。

## 怎样关闭

正常关闭黑马记账窗口即可。通过唯一启动按钮打开免安装预览版时，不需要保留额外终端窗口。

## 怎样查看Git存档

双击：

```text
00-查看Git存档点.cmd
```

它只会展示存档，不会修改文件。每行最前面的七位字符是存档编号，最上面通常是最新存档。详细说明见 [Git小白指南](docs/GIT_GUIDE.md)。

## 重要提醒

Git保存的是程序源码、测试和说明文档，不保存你在App里记录的个人账目，也不保存免安装程序等大型生成文件。个人账目要使用App内“完整备份”，并把备份复制到另一块磁盘。
