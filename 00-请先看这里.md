# 黑马记账：请先看这里

## 你现在最应该打开哪个文件

当前正式成品只有一个推荐文件：

```text
手机安装包\黑马记账-Android-正式版-1.0.1.apk
```

把它复制到安卓手机，点击后允许本次“安装未知应用”，再点击安装。安装结束后，在手机桌面找到白底记账本、笔和人民币金币图标的“黑马记账”。

这个 APK 是免联网、本地保存数据的正式版 1.0.1。它不是设计图，不是 Demo，也不是 Windows EXE。

## 文件名区别

| 名称 | 它是什么 | 是否当前推荐 |
| --- | --- | --- |
| `黑马记账-Android-正式版-1.0.1.apk` | 手机可直接安装的正式成品 | 是 |
| Android Studio 绿色三角形 | 从当前源码临时编译到模拟器 | 仅开发检查 |
| `旧版本-请勿安装` | 0.1/0.2/1.0.0 历史 APK | 否 |
| Windows `1.5.0` | 电脑桌面产品线 | 否，不是手机 App |

## 用 Android Studio 在电脑查看

1. 双击根目录 `00-用Android Studio查看手机版.cmd`。
2. 等待右下角同步和索引结束。
3. 顶部设备选择 `Heima_Android_16`。
4. 点击绿色三角形运行。
5. 检查完点击红色方块停止 App；关闭模拟器或 Android Studio 即可。

运行期间 Android Studio 和模拟器要保持打开。开发运行版本可能显示为独立调试应用，不会覆盖你安装的正式版账本。

## 数据和备份

- 账目保存在手机 App 私有数据库中，卸载 App 可能删除数据。
- 在“我的 → 数据管理”导出完整备份，并把文件复制到电脑或移动硬盘。
- 恢复会完整替换账本，但 App 会先保存恢复前副本，并校验 SHA-256 和分类关系。
- Git 只保护源码，不保护手机里的账目。

## 查看 Git 存档

双击 `00-查看Git存档点.cmd`。每行前面的 7 位字符是存档编号，最上面通常是最新存档。查看不会修改文件；需要回档时先告诉 Codex，不要自己使用会丢内容的 `reset --hard`。

更多信息见 `FINAL_RELEASE_REPORT.md`、`TEST_REPORT.md` 和 `PERFORMANCE_REPORT.md`。
