# 黑马记账：请先看这里

## 你现在最应该打开哪个文件

当前正式成品只有一个推荐文件：

```text
手机安装包\黑马记账-Android-正式版-1.1.0.apk
```

把它复制到安卓手机，点击后允许本次“安装未知应用”，再点击安装。安装结束后，在手机桌面找到白底记账本、笔和人民币金币图标的“黑马记账”。

这个 APK 是本地保存数据的正式版 1.1.0。只有用户主动点击“我的 → 检查更新”时才联网读取版本信息，不上传账单。它不是设计图，也不是 Demo。

公开更新页：`https://github.com/olo12119/heima-accounting-releases/releases/latest`。源码仓库现为公开仓库，公开页只提供安装包与说明。

## 文件名区别

| 名称 | 它是什么 | 是否当前推荐 |
| --- | --- | --- |
| `黑马记账-Android-正式版-1.1.0.apk` | 手机可直接安装的正式成品 | 是 |
| Android Studio 绿色三角形 | 从当前源码临时编译到模拟器 | 仅开发检查 |
| 1.0.3 及更早 APK | 历史成品，只用于覆盖升级验证 | 否 |

## 用 Android Studio 在电脑查看

1. 双击 `scripts\用Android-Studio查看手机版.cmd`。
2. 等待右下角同步和索引结束。
3. 顶部设备选择 `Heima_Android_16`。
4. 点击绿色三角形运行。
5. 检查完点击红色方块停止 App；关闭模拟器或 Android Studio 即可。

运行期间 Android Studio 和模拟器要保持打开。开发运行版本可能显示为独立调试应用，不会覆盖你安装的正式版账本。

## 数据和备份

- 账目保存在手机 App 私有数据库中，卸载 App 可能删除数据。
- 在“我的 → 数据管理”导出完整备份，并把文件复制到电脑或移动硬盘。
- 恢复会完整替换账本，但 App 会先保存恢复前副本，并校验 SHA-256 和分类关系。
- Git 只保护源码，不保护手机里的账目。

## 查看 Git 存档

双击 `scripts\查看Git存档点.cmd`。每行前面的 7 位字符是存档编号，最上面通常是最新存档。查看不会修改文件；需要回档时先告诉 Codex，不要自己使用会丢内容的 `reset --hard`。

更多信息见 `docs/reports/FINAL_RELEASE_REPORT.md`、`docs/reports/TEST_REPORT.md` 和 `docs/reports/PERFORMANCE_REPORT.md`。
