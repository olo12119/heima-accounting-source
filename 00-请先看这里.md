# 黑马记账：请先看这里

## 现在有两条清楚分开的路线

| 项目 | 当前状态 | 你现在能做什么 |
| --- | --- | --- |
| Windows桌面版 | 1.5.0免安装预览版，功能可用 | 在电脑上打开和体验 |
| Android手机版 | 0.3.0性能与动效基础版源码 | 用Android Studio虚拟手机运行检查 |

Windows版没有被删除，只是整齐地搬到了 `apps/windows-desktop`。Android将使用独立的Kotlin + Jetpack Compose工程，不会用Windows的Electron文件充当手机App。

## 现在如何在电脑上查看Android版（当前推荐）

双击项目根目录中的：

```text
00-用Android Studio查看手机版.cmd
```

它会用D盘里的Android Studio打开 `apps/android` 手机工程，并把SDK、Gradle缓存、虚拟手机和Android Studio的大型缓存继续放在 `D:\AndroidDev`。首次打开时需要等待右下角完成项目同步；随后在顶部设备列表选择 `Heima_Android_16`，再点击绿色三角形运行按钮。

本轮已经按你的要求停止额外生成APK。`手机安装包` 里的0.1.0和0.2.0都是历史对照版，不包含当前0.3代码。要检查本轮成果，请只走上面的Android Studio路线。

## 如何打开旧的Windows版

双击根目录里这个文件：

```text
00-打开Windows桌面版-1.5.0.cmd
```

- 这是Windows桌面产品的推荐入口，不是Android入口。
- 它会打开1.5.0免安装预览版，不需要安装。
- 打开后正常关闭应用窗口即可。
- 免安装版依赖整个 `win-unpacked` 目录，不能只复制里面的EXE。
- 如果预览成品以后被清理，该入口会改用源码启动；那时出现的命令窗口需保持开启，直到App关闭。

旧入口 `00-点我打开黑马记账-当前最新版.cmd` 只为了不让你以前的习惯失效，它现在会转到上面的新入口。

## 几个容易混淆的词

- **源码**：像做菜的菜谱和原料，是开发用的文件，不是普通用户直接打开的成品。
- **预览版**：功能已能体验，但还在等你验收的版本状态。
- **免安装版**：不需要安装，但必须保留完整文件夹。
- **安装版**：会出现Windows安装向导的成品。当前1.5.0尚未制作安装版。
- **APK**：Android手机的安装文件。本轮0.3没有额外生成；文件夹里的0.2.0只是历史版本。

## 怎样看Android设计方案和源码说明

打开 [docs/android/README.md](docs/android/README.md)，再按里面的阅读顺序查看。Android源码位于 `apps/android`；普通用户不需要阅读或运行源码。

## 怎样看Git存档

双击 `00-查看Git存档点.cmd`。它只会显示存档，不会修改项目。每行最前面的7位字符是“存档编号”，最上面通常是最新存档。

Git只保护程序源码和文档。Windows个人账目仍保存在 `%APPDATA%\HeimaAccounting\data\heima-accounting.sqlite3`，没有因为这次整理而移动。个人账目要用App内的“完整备份”另行保护。
