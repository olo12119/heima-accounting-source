# 黑马记账 Android 1.0.1 测试报告

## 最终结论

| 检查 | 结果 |
| --- | --- |
| Release Lint | ✅ 0 error、0 warning、0 hint |
| JVM 单元/数据测试 | ✅ 23/23 通过 |
| 模拟器数据库/UI 测试 | ✅ 21/21 通过 |
| R8 Release 构建 | ✅ 通过 |
| zipalign / APK v3 签名 | ✅ 通过 |
| 1.0.0 → 1.0.1 覆盖安装 | ✅ 通过，版本号 101 |
| 正式包启动冒烟 | ✅ 启动后进程存活，日志窗口无 App FATAL/ANR |

最终执行命令：

```powershell
.\gradlew.bat :app:lintRelease :core:domain:test :core:data:test :core:database:test :app:testDebugUnitTest --no-daemon
.\gradlew.bat :app:connectedDebugAndroidTest --no-daemon
.\gradlew.bat :app:lintRelease :app:assembleRelease --no-daemon
```

## 功能正确性

### 快速记账

- ✅ 输入 `12.50` 就显示 `12.50`，不会从“分”位反向推数字。
- ✅ 0、空金额、非法格式和超过两位小数不会保存。
- ✅ 支出不选一级分类会得到明确错误提示。
- ✅ 支出 → 餐饮 → 直接保存通过。
- ✅ 支出 → 餐饮 → 午餐 → 保存通过。
- ✅ 收入 → 工资 → 直接保存通过。
- ✅ 收入 → 工资 → 奖金 → 保存通过。
- ✅ 支出餐饮午餐切到收入后，支出一级/二级状态被清除；收入不会自动选分类或展开细分。
- ✅ 二级分类区域只在本次主动点击一级分类后展开，可再次取消二级选择。

### 日期与自有控件

- ✅ 日期和备注组件高度、圆角、内边距、玻璃材质与文字基线统一。
- ✅ 自有中文日期选择器可打开、换月、选择、取消、确定和重新打开。
- ✅ 默认中文星期与年月，不出现 August、Cancel、OK。
- ✅ 全项目搜索确认普通业务 UI 不再直接使用 `DatePickerDialog`、`AlertDialog`、系统 `Switch`、`OutlinedTextField` 或系统 Toast。

### 设置、隐私与统计

- ✅ Liquid Glass、操作音效、触觉反馈默认 ON 时滑块在右、使用强调色；OFF 在左、使用灰色。
- ✅ “减少动态效果”ON 的真实含义是减少动画，没有反向解释。
- ✅ 四个开关互不绑定，退出页面、Activity 重建后状态保持；本机设置文件检查确认 Liquid Glass ON/OFF 会写入真实持久化值。
- ✅ 隐藏金额在首页、统计、预算和账单同步生效，并跨页面保持。
- ✅ 统计今日/本周/本月/今年选择可切换，选中 Lens 与数据内容同步更新，计算不在主线程阻塞动画。
- ✅ 财务洞察 4 类规则测试通过：数据不足不评价、平稳正现金流、历史同比上升、预算压力。

### 数据库、分类与备份

- ✅ 默认收支两级分类、外键、WAL、索引、`quick_check` 和重开持久化。
- ✅ 新增、修改、删除、撤销及错误事务回滚。
- ✅ 二级分类必须属于所选一级分类；错误关系被数据层拒绝。
- ✅ 自定义分类可新增、改名和安全停用，不破坏历史账单。
- ✅ CSV 中文表头、UTF-8 BOM、金额与标准转义。
- ✅ JSON 备份 SHA-256、分类关系、恢复前副本和事务恢复。
- ✅ 100、1000、10000 条账单替换与读回通过。

## 视觉回归

逐屏实机模拟器截图检查覆盖：

- ✅ 首页、统计、预算、我的、设置、快速记账、二级分类和日期选择器。
- ✅ Light / Dark。
- ✅ Liquid Glass ON / OFF。
- ✅ 分类图标统一安全区与视觉重量，无资源黑色矩形边缘。
- ✅ 日期/备注等高；统计选择 Lens 连续；记账主入口醒目但不脱离连续底栏。
- ✅ 记账 Sheet 内没有背景文字穿透，关闭 Glass 后布局、圆角、颜色与功能不变。
- ✅ 深色模式无大块白色 Glow、卡片发白或文字被高光遮住。
- ✅ 点击分类不再出现与圆形图标冲突的矩形系统 Ripple。

## 开发中真实失败及修复

1. ❌ 初次编译发现作用域 Modifier 导入和动画 API 使用错误；改用当前 Compose API 后通过。
2. ❌ AI 生成的分类图集把棋盘格画进像素而非透明背景；该输出被拒绝，改用确定性 Alpha 连通域和光学重心规范化脚本。
3. ❌ 首次日期 Dialog 表面太透明，背景文字可见；增加主题 Surface 的不透明内层后重拍通过。
4. ❌ 新增 UI 测试最初按“餐饮/工资”文字点击，真实账单中出现同名文字导致 2 项测试定位失败；给分类 Chip 补充无障碍名称，并按控件语义定位，最终 21/21 通过。
5. ❌ PowerShell 默认策略阻止资源脚本；只对单次进程使用 `ExecutionPolicy Bypass`，没有修改系统安全策略，输出 SHA-256 前后完全一致。
6. ❌ 首次签名命令没有临时设置 D 盘 JDK；补充该命令的 `JAVA_HOME` 后签名成功。
7. ❌ 首次冒烟脚本把 `$PID` 当普通变量，实际是 PowerShell 保留变量；改为 `$appProcessId` 后重新采样并通过。
8. ⚠️ 性能回归期间模拟器系统 UI 自身发生 ANR、GPU 统计固定为错误值；功能/UI 测试仍可完整通过，但 FPS/Jank 标为无效，详见性能报告。

## 仍需真机验证

- ⚠️ 不同厂商启动器图标裁切、120Hz、系统字体和手势导航。
- ⚠️ 真实电池消耗、温升、厂商 GPU 驱动和低内存杀进程。
- ⚠️ 静音/勿扰的具体厂商策略。

这些项目没有被写成“已通过”，模拟器也没有冒充真实手机。
