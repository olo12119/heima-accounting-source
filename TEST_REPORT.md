# 黑马记账 Android 1.0.0 测试报告

## 最终自动检查

执行命令：

```powershell
.\gradlew.bat lintDebug testDebugUnitTest :app:connectedDebugAndroidTest assembleRelease --no-daemon
```

最终结果：

- ✅ Android Lint 严格模式：通过，未建立 Baseline 隐藏问题。
- ✅ JVM 单元测试：19 项通过，0 失败、0 错误、0 跳过。
- ✅ 模拟器集成/UI 测试：14 项通过，0 失败、0 跳过。
- ✅ Release 构建：通过，R8 代码压缩和资源收缩完成。
- ✅ APK zipalign、v3 签名验证和覆盖安装：通过。

## 覆盖范围

### 金额与规则

- ✅ `12.50`、整数、单个小数点、最多两位小数和整数分转换。
- ✅ 0 元和非法金额不会保存。
- ✅ 收入/支出切换后分类集合同步变化。
- ✅ 日期范围、本周周一至周日、分类聚合和问候语稳定轮换。

### 数据库与账单

- ✅ 首次迁移、完整默认分类、外键、索引和快速完整性检查。
- ✅ 新增支出、新增收入、编辑、删除、撤销、关闭后重开仍保留。
- ✅ 二级分类必须属于正确一级分类。
- ✅ 自定义分类添加、改名和带历史账单的安全停用。
- ✅ 事务错误回滚，不留下半份数据。
- ✅ 100、1000、10000 条账单替换和读取测试通过。

### 备份与导出

- ✅ CSV 中文表头、BOM、金额元格式和标准转义。
- ✅ JSON 备份导出、SHA-256 校验、恢复回读。
- ✅ 错误校验值、错误分类关系和非法数据拒绝恢复。
- ✅ 恢复前安全备份和事务替换逻辑已覆盖。

### UI 与设置

- ✅ 五个底部入口可达，快速记账可打开。
- ✅ 金额按用户输入原样显示，收入/支出和分类交互可用。
- ✅ 真实支出保存后首页数据更新。
- ✅ 两套主题、浅色/深色/跟随系统状态切换。
- ✅ Liquid Glass、音效、触觉相互独立并永久保存。
- ✅ 金额隐藏跨页面与 Activity 重建保持一致。
- ✅ 空账本没有虚假交易。

## 异常与压力检查

- ✅ 连续 Tab 切换不再同时组合多个图表页面。
- ✅ 快速记账 Drag Handle 支持跟手、阈值关闭、速度关闭和回弹。
- ✅ 大数据量 100/1000/10000 条不会破坏数据库关系。
- ✅ 数据库替换中的外键失败能够回滚。
- ✅ App 杀掉再启动后账目和偏好仍从本地恢复。
- ✅ 深色首页、统计、预算和快速记账逐屏截图检查，无黑屏、闪白或文字被大面积 Glow 覆盖。

## 开发中发现并修复的真实问题

1. ❌ 初版恢复事务先删父分类，触发分类自关联外键失败。修复为先删子分类再删父分类，并重跑 14 项测试通过。
2. ❌ 初版 Backdrop 录制层发生递归采样并触发原生崩溃。修复为只录制稳定环境背景，Glass 控件不反向进入自己的采样源。
3. ❌ 初版全屏大玻璃层导致 Tab 压力场景卡顿。修复为只在重要小表面启用真实 Blur/Lens，其余使用统一回退材质。
4. ❌ 3D 图集曾在多个分类图标中重复解码。修复为 App 级单实例缓存；Lint 又发现资源读取不随 Configuration 更新，随后改用 `LocalResources`。
5. ❌ 快速记账面板长弹簧会持续重绘密集表单。修复为短距离 90ms 上推；拖动未达阈值的 Spring 回位继续保留。
6. ❌ 隐私 UI 测试最初假定页面只有一处金额，实际产品正确隐藏了多处。修正测试为检查整个敏感金额集合，没有删除产品功能。
7. ❌ PowerShell 执行策略阻止本地脚本。仅对当前进程使用 `-ExecutionPolicy Bypass`，没有修改系统永久策略。
8. ❌ 最终门禁初次重跑被模拟器残留的旧 Debug/测试包签名阻止。先备份旧开发数据库，再精确卸载两个冲突包；第三次完整门禁 14/14 模拟器测试通过。

## 未完全自动化的项目

- ⚠️ 不同厂商手机的启动器裁切、手势冲突、系统字体和 120Hz 显示：需要用户真机验收。
- ⚠️ 真实低内存杀进程和厂商后台限制：模拟器只完成基础前后台/重启检查。
- ⚠️ 勿扰模式与各厂商静音策略：代码遵守 Android 正常音频行为，但仍需具体手机核对。
- ⚠️ 商店安装、应用商店升级和代码签名托管：本项目没有上架商店。

测试报告不把上述项目写成“通过”，也不把模拟器冒充真机。
